import { initializeParallax } from './parallax';

document.addEventListener('DOMContentLoaded', () => {
  const container = document.querySelector('[data-parallax-container]');
  if (!container) {
    return;
  }
  // initializeParallax(container);
});
